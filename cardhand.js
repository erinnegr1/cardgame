
//Write a Card class with two properties:

class Card {
    constructor(rank, suit) {
        this.rank = rank;
        this.suit = suit;
    }
}

// create a class that is an empty array of card objects
class Hand {
    constructor() {
        this.cards = [];
    }
}

// will add a card to the empty array
addCard(card) 
    this.cards.push(card);

// will remove a card to the empty array
playCard(card)
    this.cards.slice(card)


const hand1 = Hand
const card1 = Card;
Hand.addCard(card)
